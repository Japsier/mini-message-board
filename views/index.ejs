<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <a href="http://localhost:3000/new"><button>Add Message</button></a>
    <% for (let i = 0; i < messages.length; i++) { %>
      
       <li>
        <div class="Name">
          <%= messages[i].text %>
        </div>
        <div class="Name">
          <span>From: </span>
          <span><%= messages[i].user %></span>    
        </div>
        <div class="Date">
          <span>Posted on: </span>
          <span><%= messages[i].added %></span> 
        </div>
      </li>
    <% } %>
  </body>
</html>
